#include <stdio.h>
#include <stdbool.h>
#include <stdlib.h>
#include <math.h>

int main(){
    char nome[10]; // vetor de 10 caracteres
    scanf("%s", nome);
    printf("nome = %s", nome);

    return 0;
}